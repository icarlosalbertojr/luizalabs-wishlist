server:
    port: 8080

spring:
  profiles:
    active: ${WISHLIST_ENVIRONMENT:local}
  application:
    name: wishlist
  data:
    mongodb:
      uri: ${WISHLIST_MONGODB_URL}
      auto-index-creation: true
service:
  params:
    wishlist:
      maxLimit: ${WISHLIST_MAX_LIMIT:20}

